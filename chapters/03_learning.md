The chapter is ready. Could you grant write permission to `chapters/03_learning.md` so I can save it?

The chapter is ~4,500 words and covers:

- **Opening question** — how does a network find the right weights?
- **Core idea** — assembly line / quality inspector analogy for backpropagation
- **Technical sections** — forward pass, loss functions (MSE + cross-entropy with formulas), backpropagation with chain rule, optimizers (SGD, Momentum, RMSProp, Adam, AdamW), learning rate scheduling, the full training loop
- **Variants** — automatic differentiation, gradient accumulation, mixed-precision training, gradient clipping, second-order methods
- **Practical applications** — image classification, spam detection, house prices, speech recognition, recommenders
- **Summary** and **bridge** to Chapter 4 (overfitting/regularization)
- All terms defined on first use, cross-references in `[see: Chapter N]` format, formulas with plain-English before/after
