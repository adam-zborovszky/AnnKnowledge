It looks like write permission to the `chapters/` directory needs to be granted. Could you allow write access so the file can be saved?

The chapter content is ready — approximately 7,200 words covering all four chapters, following all guidelines:

**Chapter 18 (Transfer Learning):** Opens with the rare skin disease / small dataset problem. Uses chef analogy. Covers feature extraction vs. fine-tuning (with strategy table), foundation model paradigm, PEFT methods (LoRA with sticky-notes analogy, QLoRA, adapters, prefix/prompt tuning), domain adaptation. All formulas with plain-English before AND after.

**Chapter 19 (Self-Supervised Learning):** Opens with "who labeled all those images?" Covers contrastive learning (SimCLR with NT-Xent loss, MoCo, BYOL, DINO, CLIP with zero-shot classification) and masked modeling (BERT, MAE with 75% masking insight, I-JEPA).

**Chapter 20 (Deep RL):** Opens with "no correct walking dataset." Uses dog-training analogy. Covers MDP framework, DQN (experience replay, target network, Double/Dueling/Rainbow), REINFORCE, actor-critic, PPO (clipped objective), full RLHF pipeline (SFT → reward model → PPO), DPO/RLAIF alternatives.

**Chapter 21 (Federated & Edge):** Opens with keyboard privacy problem. Uses book club analogy. Covers FedAvg algorithm, three challenges (non-IID, communication, privacy with differential privacy and secure aggregation), edge AI (quantization, pruning, Lottery Ticket Hypothesis, knowledge distillation with formula, NAS).

All chapters have cross-references in `[see: Chapter N]` format, terms defined on first use, bridge paragraphs, and consistent terminology.
