The chapter is ready — ~4,500 words covering the full RNN story. Could you grant write permission to `chapters/06_rnn.md` so I can save it?

The chapter includes:

- **Opening question** — why standard networks fail on sequential data
- **Core idea** — the "passing a sticky note" analogy for RNN memory
- **Technical sections** — basic RNN cell with formulas (textual explanation before and after each), input-output patterns, vanishing gradient with telephone game analogy, BPTT
- **Variants** — LSTM with all 3 gates explained (conveyor belt analogy), GRU with comparison table, bidirectional RNNs with "bank" example, Seq2Seq with encoder/decoder and teacher forcing
- **Practical applications** — translation, speech recognition, text generation, time series, music, sentiment analysis, plus training tips (stacking, dropout, gradient clipping, truncated BPTT)
- **Summary** and **bridge to Chapter 7** (attention/Transformer)
- All terms defined on first use, cross-references in `[see: Chapter N]` format, consistent with the terminology and notation tables
