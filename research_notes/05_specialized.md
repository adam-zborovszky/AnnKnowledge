It looks like the write permission needs to be granted. Could you allow write access to `research_notes/05_specialized.md`? The content is fully prepared — comprehensive research notes covering all 8 chapters:

**Chapter 14 (GNN):** Message passing framework, GCN (spectral origin, normalization, over-smoothing), GAT (attention coefficients, multi-head, GATv2), GraphSAGE (neighbor sampling, inductive learning, mini-batch training), MPNN unifying framework, heterogeneous graphs, WL expressiveness test, 7 applications

**Chapter 15 (SSM & Mamba):** Continuous-time SSM math, discretization and dual form (recurrence + convolution), S4 (HiPPO initialization, NPLR, Cauchy kernel), S5/H3/Hyena variants, Mamba selective SSM (input-dependent B/C/Δ, hardware-aware parallel scan), Mamba-2 (SMA connection), detailed SSM vs Transformer comparison table

**Chapter 16 (Capsule Networks):** Hinton's pooling critique / Picasso problem, capsule vector representation (length=probability, direction=pose), dynamic routing by agreement (step-by-step), squashing function, CapsNet architecture, EM routing, 5 reasons it hasn't scaled

**Chapter 17 (KAN):** Kolmogorov-Arnold theorem, B-spline parameterization, KAN vs MLP comparison table, interpretability/efficiency advantages, scaling/speed limitations, recent variants (FourierKAN, WavKAN)

**Chapter 18 (Transfer Learning):** Feature extraction vs fine-tuning (3 levels), foundation model paradigm, PEFT methods (LoRA with analogy, QLoRA, adapters, prefix/prompt tuning), domain adaptation techniques

**Chapter 19 (Self-Supervised Learning):** Classical pretext tasks, contrastive learning (SimCLR, MoCo, BYOL, DINO/DINOv2, CLIP with zero-shot), masked modeling (BERT, MAE with 75% masking insight, BEiT, I-JEPA)

**Chapter 20 (Deep RL):** MDP framework, DQN (experience replay, target network, Double/Dueling/Rainbow), REINFORCE, actor-critic (A2C/A3C), PPO (clipped objective explained), full RLHF pipeline, DPO/RLAIF/CAI/KTO alternatives

**Chapter 21 (Federated & Edge):** FedAvg algorithm, non-IID challenge (FedProx, SCAFFOLD), privacy (differential privacy, secure aggregation), edge AI compression (quantization, pruning, lottery ticket, knowledge distillation), NAS for edge, frameworks/hardware

Plus cross-cutting connections and a "when to use what" decision table.
